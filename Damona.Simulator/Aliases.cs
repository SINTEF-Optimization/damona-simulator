// Specialty is coded as an integer for performance. Mapping between id and name is done using the SpecialtyMap class.
// There is a special Id used for emergency only surgeries, this value is defined in Constants.

global using Specialty = int;
global using Urgency = int;